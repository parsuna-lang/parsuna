[project]
name = "parsuna-docs"
version = "0.0.0"
description = "Build dependencies for the parsuna Sphinx documentation."
requires-python = ">=3.9"
dependencies = [
    "sphinx>=9",
    "furo>=2024.1",
]
